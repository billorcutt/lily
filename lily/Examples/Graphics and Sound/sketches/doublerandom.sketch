//http://processing.org/learning/basics/doublerandom.html

size(200, 200);
background(0);
var totalPts = 300;
var steps = totalPts+1;
stroke(255);
var rand = 0;

for  (var i=1; i< steps; i++){
  point( (width/steps) * i, (height/2) + random(-rand, rand) );
  rand += random(-5, 5);
}